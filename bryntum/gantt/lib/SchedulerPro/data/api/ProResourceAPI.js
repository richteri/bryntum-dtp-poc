import Base from '../../../Core/Base.js';

/**
 * @module Scheduler/data/api/ProResourceAPI
 */

/**
 * Pro resource model data API mixin
 *
 * The mixin should be mixed alongside with other API mixins, because it might rely on them.
 *
 * @mixin
 */
export default Target => class ProResourceAPI extends (Target || Base) {
    // NOTE: Add pro api override methods here
};
